# ── Serveur ──────────────────────────────
PORT=3000

# ── MySQL ────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ton_mot_de_passe
DB_NAME=sportbook

# ── JWT ──────────────────────────────────
JWT_SECRET=change_this_secret_key_in_production
JWT_EXPIRES_IN=7d

# ── SMTP (optionnel — lien affiché dans la console si non configuré) ──
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre_utilisateur_mailtrap
SMTP_PASS=votre_mot_de_passe_mailtrap
SMTP_FROM=noreply@sportbook.app

# ── API ──────────────────────────────────
API_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:5500,http://localhost:5501
